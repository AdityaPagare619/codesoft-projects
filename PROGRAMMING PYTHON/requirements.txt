# Requirements for CodSoft Python Tasks
# All dependencies use only Python standard library for maximum compatibility

# No external dependencies required - all tasks use built-in Python modules:
# - tkinter (GUI framework - included with Python)
# - json (data persistence)
# - sqlite3 (database for contacts)
# - secrets (secure password generation)
# - ast (safe expression evaluation)
# - datetime (date/time handling)
# - os (file operations)
# - pathlib (modern path handling)

# Optional: For enhanced functionality (not required)
# pyperclip>=1.8.2  # Enhanced clipboard operations
# pillow>=9.0.0     # Image handling for contact photos